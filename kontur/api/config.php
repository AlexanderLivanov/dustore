<?php
// ============================================================
// CONFIG.PHP — настройки подключения к БД
// ============================================================
// ВАЖНО: этот файл лежит ВНЕ public/ (docroot Apache),
// поэтому его нельзя запросить напрямую из браузера.
// В реальном проекте секреты стоит держать в переменных
// окружения (getenv), а не хардкодить.
// ============================================================

return [
    'db' => [
        'host'    => '127.0.0.1',
        'port'    => 3306,
        'name'    => 'kontur',
        'user'    => 'root',   // создайте отдельного пользователя, НЕ root
        'pass'    => '',     // смените пароль!
        'charset' => 'utf8mb4',
    ],

    // Разрешённый источник для CORS (если фронт и API на разных портах).
    // Если всё на одном домене — оставьте пустым.
    'cors_origin' => '',

    // Режим отладки: показывать подробные ошибки. В продакшене — false.
    'debug' => true,
];
